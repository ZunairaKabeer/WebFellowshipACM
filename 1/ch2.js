/* challenge #1
this code doesnt work because we cannot increment a constant variable
*/

/* challenge #2
yes because both code snippets are exactly the same
and I would recommend using neither because both would return false
*/

// challenge #3
// the expression5 evaluates to false
// we can combine this:
const result = !(((100 % 50) < (100 / 50)) && 300 + 5 === 305);
console.log(result);

// challenge #4
// the result evaluates to true

// challenge #5
// the result evaluates to true